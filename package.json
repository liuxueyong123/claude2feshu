{
  "name": "claude2feishu",
  "version": "2.0.0",
  "description": "Claude Code ↔ 飞书双向通信",
  "type": "module",
  "scripts": {
    "start":        "npx tsx src/scripts/service.ts start",
    "stop":         "npx tsx src/scripts/service.ts stop",
    "status":       "npx tsx src/scripts/service.ts status",
    "logs":         "npx tsx src/scripts/service.ts logs",
    "restart":      "npx tsx src/scripts/service.ts restart",
    "once":         "curl -s -X POST http://127.0.0.1:${FEISHU_NOTIFYD_PORT:-9876}/daemon/once | python3 -m json.tool 2>/dev/null || echo '(check status)'",
    "inbox":        "curl -s http://127.0.0.1:${FEISHU_NOTIFYD_PORT:-9876}/inbox",
    "pop":          "curl -s -X POST http://127.0.0.1:${FEISHU_NOTIFYD_PORT:-9876}/inbox/pop",
    "clear":        "curl -s -X POST http://127.0.0.1:${FEISHU_NOTIFYD_PORT:-9876}/inbox/clear | python3 -m json.tool 2>/dev/null || echo '(check status)'",
    "session-list": "curl -s http://127.0.0.1:${FEISHU_NOTIFYD_PORT:-9876}/sessions | python3 -m json.tool 2>/dev/null || echo '(check status)'",
    "test-webhook": "curl -s -X POST http://127.0.0.1:${FEISHU_NOTIFYD_PORT:-9876}/test/webhook | python3 -m json.tool 2>/dev/null || echo '(check status)'",
    "test-api":     "curl -s -X POST http://127.0.0.1:${FEISHU_NOTIFYD_PORT:-9876}/test/api | python3 -m json.tool 2>/dev/null || echo '(check status)'",
    "setup-hooks":  "npx tsx src/scripts/setup-hooks.ts",
    "test":         "node --import tsx --test tests/*.test.ts",
    "build":        "npx tsc",
    "typecheck":    "npx tsc --noEmit"
  },
  "devDependencies": {
    "@types/koa": "^3.0.3",
    "@types/koa-bodyparser": "^4.3.13",
    "@types/koa__router": "^12.0.5",
    "@types/node": "^25.9.2",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@koa/router": "^15.6.0",
    "dotenv": "^17.4.2",
    "koa": "^3.2.1",
    "koa-bodyparser": "^4.4.1"
  }
}
